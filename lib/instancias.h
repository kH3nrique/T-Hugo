#include <iostream>
#include <fstream>
#include <vector>
#include <time.h>

#define V 100

using namespace std;

int safe(int graf[V][V], int tam){
    for (int i = 0; i < tam; i++){
        for (int j = 0; j < tam; j++){
            if (i == i + 2 && j == j + 2){
                
            }
        }
    }
}